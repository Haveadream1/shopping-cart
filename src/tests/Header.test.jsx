const { renderMatches } = require("react-router");
const { describe, it } = require("vitest");

describe("Header component", () => {
    it("renders correct heading", () => {
        render()
    })
})